# 作业登记簿设计说明

## 视觉设计

### 书本卡片拟物化效果

```
┌─────────────────────────────────────┐
│  ╔═│                                 │  ← 书脊 (12px 宽)
│  ╔═│  ┌─────────────────────┐      │
│  ╔═│  │ 书本标题            │      │
│  ╔═│  │ 2024-01-04 作业     │      │
│  ╔═│  │                     │      │
│  ╔═│  │ 3/5                 │      │  ← 进度显示
│  ╔═│  │ ████████░░░░        │      │  ← 进度条
│  ╔═│  │                     │      │
│  ╔═│  │ 🕐 今天             │      │  ← 截止时间
│  ╔═│  │              [✏️]   │      │  ← 编辑按钮 (hover)
│  ╔═│  └─────────────────────┘ ▒▒   │  ← 书页 (右侧条纹)
└─────────────────────────────────────┘
```

### 颜色系统

#### 状态颜色

| 状态 | 主色 | 浅色 | 深色 | 用途 |
|------|------|------|------|------|
| 草稿 | `#9CA3AF` | `#D1D5DB` | `#6B7280` | 书脊、进度条 |
| 进行中 | `#F59E0B` | `#FDE68A` | `#D97706` | 书脊、进度条 |
| 已完成 | `#34D399` | `#6EE7B7` | `#059669` | 书脊、进度条 |

#### CSS 变量

使用 `styles.css` 中定义的变量：
```css
--color-primary: #F59E0B          /* 主色调 - 温暖琥珀橙 */
--color-text-primary: #1F2937     /* 主要文字 */
--color-text-secondary: #78716C   /* 次要文字 */
--color-text-muted: #9CA3AF       /* 弱化文字 */
```

### 3D 悬停效果

```css
/* 默认状态 */
transform: none;
box-shadow:
    0 2px 8px rgba(0, 0, 0, 0.08),
    inset 0 -2px 6px rgba(0, 0, 0, 0.02);

/* hover 状态 */
transform: translateY(-8px) rotate(1deg);
box-shadow:
    0 12px 24px rgba(0, 0, 0, 0.12),
    inset 0 -2px 6px rgba(0, 0, 0, 0.02);

/* active 状态 */
transform: translateY(-4px) rotate(0.5deg);
```

## 响应式布局

### PC (≥1024px)

```
┌──────────────────────────────────────────────────────┐
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐               │
│  │ 书1  │ │ 书2  │ │ 书3  │ │ 书4  │               │
│  │ 3/5  │ │ 0/2  │ │ 5/5  │ │ 1/3  │               │
│  └──────┘ └──────┘ └──────┘ └──────┘               │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐               │
│  │ 书5  │ │ 书6  │ │ 书7  │ │  +   │               │
│  └──────┘ └──────┘ └──────┘ └──────┘               │
└──────────────────────────────────────────────────────┘

- 列数: 4
- 初始加载: 7 个
- 滚动加载: 4 个
- 间距: 1.5rem (24px)
```

### 平板 (768-1023px)

```
┌────────────────────────────────────┐
│  ┌──────┐ ┌──────┐ ┌──────┐      │
│  │ 书1  │ │ 书2  │ │ 书3  │      │
│  └──────┘ └──────┘ └──────┘      │
│  ┌──────┐ ┌──────┐ ┌──────┐      │
│  │ 书4  │ │ 书5  │ │  +   │      │
│  └──────┘ └──────┘ └──────┘      │
└────────────────────────────────────┘

- 列数: 3
- 初始加载: 5 个
- 滚动加载: 3 个
- 间距: 1.25rem (20px)
```

### 移动 (<768px)

```
┌──────────────────┐
│  ┌──────┐ ┌──────┐│
│  │ 书1  │ │ 书2  ││
│  └──────┘ └──────┘│
│  ┌──────┐ ┌──────┐│
│  │ 书3  │ │  +   ││
│  └──────┘ └──────┘│
└──────────────────┘

- 列数: 2
- 初始加载: 3 个
- 滚动加载: 2 个
- 间距: 1rem (16px)
```

## 交互设计

### 点击区域

```
┌─────────────────────────────┐
│                     [✏️]    │  ← 编辑按钮 (点击)
│                             │
│      📖 书本内容区域        │  ← 点击跳转详情
│                             │
└─────────────────────────────┘
```

### 长按菜单 (移动端)

```
1. 用户长按书本 (800ms)
2. 显示确认对话框:
   ┌─────────────────────────┐
   │  2024-01-04 作业        │
   │                        │
   │  确定 → 编辑           │
   │  取消 → 查看详情        │
   └─────────────────────────┘
```

### 加载动画

```
时间轴:
0ms    : 卡片从下方淡入
50ms   : 第2张卡片淡入
100ms  : 第3张卡片淡入
150ms  : 第4张卡片淡入
...
300ms  : 第7张卡片淡入

动画曲线: cubic-bezier(0.34, 1.56, 0.64, 1)
效果: 从 translateY(20px) + scale(0.95) 到 正常位置
```

## 截止时间显示逻辑

```
今天 (2024-01-04)
├─ 2024-01-04 → "今天"
├─ 2024-01-05 → "明天"
├─ 2024-01-03 → "昨天"
├─ 2024-01-08 → "周一" (7天内)
├─ 2024-01-15 → "1/15"  (超过7天)
└─ null → "未设置"
```

## 进度计算

```
总作业数: 5
已完成: 3
未完成: 2

进度文本: "3/5"
进度百分比: 60%
进度条宽度: 60%
```

## 性能优化

### 防抖策略

```javascript
// 滚动事件 - 100ms 防抖
window.addEventListener('scroll', () => {
    clearTimeout(scrollTimeout);
    scrollTimeout = setTimeout(checkScrollAndLoad, 100);
});

// 窗口 resize - 200ms 防抖
window.addEventListener('resize', () => {
    clearTimeout(resizeTimeout);
    resizeTimeout = setTimeout(updateGridColumns, 200);
});
```

### 长按优化

```javascript
// 触摸开始
touchstart → 启动 800ms 定时器

// 触摸移动 → 取消长按
touchmove → 清除定时器

// 触摸结束 → 清除定时器
touchend → 清除定时器
```

## 可访问性

### 减少动画模式

```css
@media (prefers-reduced-motion: reduce) {
    .book-card {
        animation: none;
        transition: none;
    }
}
```

### 高对比度模式

```css
@media (prefers-contrast: high) {
    .book-card {
        border: 1px solid rgba(0, 0, 0, 0.2);
    }
}
```

### 触摸设备优化

```css
@media (hover: none) {
    .book-card:active {
        transform: scale(0.97);
    }
}
```

## 浏览器兼容性

| 特性 | Chrome | Firefox | Safari | Edge |
|------|--------|---------|--------|------|
| CSS Grid | 57+ | 52+ | 10.1+ | 16+ |
| aspect-ratio | 88+ | 89+ | 15+ | 88+ |
| backdrop-filter | 76+ | 103+ | 9+ | 76+ |
| CSS Variables | 49+ | 31+ | 9.1+ | 15+ |

## 文件结构

```
frontend/
├── js/
│   └── registry.js          (484 行)
│       ├── 状态管理
│       ├── 响应式配置
│       ├── DOM 元素
│       ├── 初始化
│       ├── 数据加载
│       ├── 渲染
│       ├── HTML 生成
│       ├── 计算辅助
│       ├── 事件绑定
│       ├── 事件处理
│       ├── 导航
│       └── 工具
│
├── css/
│   └── registry.css         (450 行)
│       ├── 网格容器
│       ├── 书本卡片
│       ├── 书脊效果
│       ├── 书页效果
│       ├── 内容区域
│       ├── 进度条
│       ├── 截止时间
│       ├── 编辑按钮
│       ├── 添加按钮
│       ├── 空状态
│       ├── 响应式
│       ├── 动画
│       ├── 状态颜色
│       └── 辅助类
│
├── registry-test.html       (测试页面)
├── REGISTRY_README.md       (功能说明)
└── INTEGRATION_GUIDE.md     (集成指南)
```
